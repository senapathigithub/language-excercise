def m(arr):
    for i in range(len(arr)):
       if arr[i]<=arr[i-1]:
          return False
    return True
my_list[2,3,5,6,7,8,9]
res =m(my_list)
if result:
   print("sorted")
else:
   print("unsorted")
